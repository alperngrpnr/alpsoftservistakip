<Page x:Class="alpsoftservistakip.PageDisServis"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:alpsoftservistakip.Controls"
      mc:Ignorable="d">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#3498DB"/>
        <!-- Window7.xaml içindeki tüm stil tanımları buraya taşındı -->
        <Style TargetType="{x:Type DatePicker}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#DCDDE1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="IsTodayHighlighted" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePicker}">
                        <Grid x:Name="PART_Root">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <DatePickerTextBox x:Name="PART_TextBox" 
                                                     Grid.Column="0"
                                                     Foreground="{TemplateBinding Foreground}"
                                                     Background="Transparent"
                                                     BorderThickness="0"
                                                     Padding="10,0"
                                                     VerticalContentAlignment="Center"/>

                                    <Button x:Name="PART_Button" 
                                          Grid.Column="1"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Width="40"
                                          Cursor="Hand"
                                          Focusable="False">
                                        <Path Data="M19,4H5A2,2 0 0,0 3,6V20A2,2 0 0,0 5,22H19A2,2 0 0,0 21,20V6A2,2 0 0,0 19,4M19,20H5V10H19V20M5,8V6H19V8H5M7,12H12V17H7V12Z" 
                                              Fill="#3498DB" 
                                              Stretch="Uniform" 
                                              Width="18" 
                                              Height="18"/>
                                    </Button>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" 
                                   AllowsTransparency="True" 
                                   Placement="Bottom" 
                                   PlacementTarget="{Binding ElementName=PART_Root}" 
                                   StaysOpen="False"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Root" Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#3498DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeight" Value="55"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#EDF2F7"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#718096"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E1F0FA"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8FAFC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C0392B"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#A93226"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#F4F7F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,20,0,0">
            <StackPanel Orientation="Horizontal">
                <Border Width="5" Height="35" Background="{StaticResource PrimaryColor}" Margin="0,0,15,0"/>
                <TextBlock Text="Dış Servis Kayıtları" FontSize="30" FontWeight="ExtraBold" Foreground="#2C3E50"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="20,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border CornerRadius="20" Background="White" BorderBrush="#DCDDE1" BorderThickness="1" Width="300" Height="40">
                    <TextBox x:Name="aramadisservis1" Text="{Binding AramaMetni, UpdateSourceTrigger=PropertyChanged}" 
                             GotFocus="aramadisservis1_GotFocus" LostFocus="aramadisservis1_LostFocus" 
                             Background="Transparent" 
                             BorderThickness="0" VerticalContentAlignment="Center" Padding="15,0" Foreground="#7F8C8D"/>
                </Border>

                <Border Background="White" CornerRadius="10" Padding="15,8" Margin="20,0,0,0" BorderBrush="#DCDDE1" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <DatePicker x:Name="dtpServisTarihi" 
                                    Width="180"
                                    SelectedDate="{Binding SecilenTarih, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Border>

                <Button x:Name="btnFiltreTemizle" 
                        Content="🗑 Filtreyi Kaldır" 
                        Margin="15,0,0,0" 
                        Height="40" 
                        Width="150"
                        Style="{StaticResource ModernButtonStyle}"
                        Command="{Binding FiltreTemizleCommand}"/>

                <Button x:Name="btnFisiYazdir" 
                        Content="📄 FİŞ YAZDIR" 
                        Margin="15,0,0,0" 
                        Height="40" 
                        Width="150"
                        Background="#27AE60"
                        Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold"
                        FontSize="13"
                        Cursor="Hand"
                        Command="{Binding FisiYazdirCommand}"
                        CommandParameter="{Binding SelectedItem, ElementName=dgdisservisVeriler}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <Button x:Name="geridöndisservis" Content="← GERİ DÖN" FontWeight="Bold" Foreground="White" 
                    Background="#252B36" HorizontalAlignment="Right" VerticalAlignment="Center" 
                    Width="160" Height="40" Command="{Binding GeriDonCommand}">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="20"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>

        <Border Grid.Row="2" Background="White" Margin="20,0,20,20" CornerRadius="10" Padding="10">
            <DataGrid x:Name="dgdisservisVeriler"
                      AutoGenerateColumns="False"
                      ItemsSource="{Binding Veriler}"
                      MouseDoubleClick="disMouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="DIŞ SERVİS TARİHİ" 
                                      Binding="{Binding KayitTarihi, StringFormat=\{0:dd.MM.yyyy\}}" 
                                      Width="1.1*" MinWidth="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="MÜŞTERİ ADI" 
                                      Binding="{Binding IsimSoyisim}" 
                                      Width="1.3*" MinWidth="180">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="MARKA" 
                                      Binding="{Binding Marka}" 
                                      Width="0.8*" MinWidth="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="MODEL" 
                                      Binding="{Binding Model}" 
                                      Width="1*" MinWidth="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="ARIZA DETAYI" 
                                      Binding="{Binding Ariza}" 
                                      Width="1.6*" MinWidth="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextAlignment" Value="Left"/>
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="Padding" Value="10,5"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <local:OverlayHost x:Name="OverlayHost" Panel.ZIndex="999"/>
    </Grid>
</Page>



